import tkinter as tk
import base64

#initialize window

WINDOW = tk.Tk()

WINDOW.geometry('500x300')
WINDOW.resizable(0,0)
WINDOW.title("WeSellAnyCode - Message Encode Decode")

#tk.Label(WINDOW, text ='ENCODE DECODE', font = 'arial 20 bold').pack()
#tk.Label(WINDOW, text ='DataFlair', font = 'arial 20 bold').pack(side = BOTTOM)